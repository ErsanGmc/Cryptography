# Cryptography.py

## Description
This is a Python library for performing various cryptographic operations. It includes functionality for working with both the Turkish and English alphabets.

## Installation
To install the library, you can use pip:

```bash
pip install cryptography.py


from cryptography import modInverse
modInverse(A, M): 
This function calculates the modular multiplicative inverse of A modulo M.